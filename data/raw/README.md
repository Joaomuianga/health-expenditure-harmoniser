Place the assessment files here (unzip `candidate_data.zip` into this folder):

- country_a_expenditure.csv
- country_b_depenses.xlsx
- country_c_expenditure.json

Raw data is git-ignored on purpose: it is not part of the source repository.
The `ref_*.csv` reference files are embedded (as seed tables) under `config/`.
